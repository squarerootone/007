# Claude Code Harness

Claude Code-specific adapters live here.

Reusable skills should stay aligned with `opencode/skills` unless Claude-specific metadata is required.

For a project that uses `AGENTS.md`, create a thin `CLAUDE.md` adapter:

```md
@AGENTS.md

# Claude Code Notes

Claude-specific notes go here.
```
